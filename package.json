{
  "dependencies": {
    "@types/leaflet": "^1.9.19",
    "leaflet": "^1.9.4",
    "node-cron": "^4.2.0",
    "react-leaflet": "^4.2.1"
  },
  "name": "VayuDrishti",
  "private": true,
  "scripts": {
    "build": "npm --prefix client run build && npm --prefix aqi-backend run build"
  }
}
